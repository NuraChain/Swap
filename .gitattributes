# Normalize line endings across platforms - the ESLint gate enforces LF
# (`linebreak-style: unix`), so a CRLF checkout must never happen.
* text=auto eol=lf

# Runtime artifacts and true binaries (defensive: most are gitignored).
*.db binary
*.db-shm binary
*.db-wal binary
*.png binary
*.ico binary
